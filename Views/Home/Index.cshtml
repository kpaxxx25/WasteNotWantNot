@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@model List<WNWN.Models.Ingredients>
{
    ViewData["Title"] = "Home Page";

@if (SignInManager.IsSignedIn(User))
{
    <!DOCTYPE html>
    <html>
    <head>
        <style>
            body {
                background-color: teal;
            }

            h1 {
                color: black;
            }

            p {
                color: black;
            }
        </style>
    </head>
    <body>
        <h1>Ingredient List</h1>

        <p>
            <a asp-controller="Ingredients" asp-action="Add">Add Ingredient</a>
        </p>


        <p>
            <a asp-controller="Ingredients" asp-action="Delete">Delete Ingredient</a>
        </p>
@*
        @if (Model.Count == 0)
        {
            <p>Your digital pantry is empty.</p>
        }
        else
        {
            <table class="table">
                <tr>
                    <th>
                        Id
                    </th>
                    <th>
                        Name
                    </th>
                    <th>
                        Food Group
                    </th>
                    <th>
                        Weight
                    </th>
                    <th>
                        Expiration Date
                    </th>
                </tr>
                @foreach (var ing in Model)
                {
                    <tr>
                        <td>@ing.Id</td>
                        <td>@ing.Name</td>
                        <td>@ing.Group</td>
                        <td>@ing.Weight</td>
                        <td>@ing.ExpirationDate</td>
                    </tr>
                }
            </table>
        }
*@
    </body>
</html>
}
else
{
@*Show pictures here.*@ <p>Log in to access your digital pantry. New? Our Register button is up above. All you need is a kitchen scale, your kitchens ingredients, and their expiration dates to keep track of your food to prevent waste.</p>
}
}